# Example environment file for jira-auto-worklog
# Copy this file to `.env` and fill in your actual credentials.

# Jira base URL (e.g. https://yourcompany.atlassian.net)
JIRA_URL=

# Default Jira issue/key (fallback si el mes no está en JIRA_ISSUE_MAP_JSON)
JIRA_ISSUE=
# Mapping por año (YYYY -> ISSUEKEY).
# Mapping por mes (YYYY-MM -> ISSUEKEY). El mes tiene prioridad sobre el año
# Usar JSON válido.
# Ejemplo: {"2025-12":"PROY-111","2026-01":"PROY-112","2026":"PROY-113"}
JIRA_ISSUE_MAP_JSON=

# Jira session cookie (OBLIGATORIA para cuentas corporativas con SSO)
JIRA_COOKIE=JSESSIONID=<jsessionid>; tenant.session.token=<jwt_token>;

# Horas de trabajo de lunes a jueves 
HORAS_DIARIAS_NORMAL=8.5  
# Horas de trabajo día viernes
HORAS_DIARIAS_REDUCIDA=6

# Comentario del worklog (usar genérico en este archivo de ejemplo)
COMENTARIO=